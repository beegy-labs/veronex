# Tasks: 2026-Phase3 — ProcessManager

> L3: ADD 자율 실행 | scopes/2026-Phase3.md 기반 | **Created**: 2026-05-09
> **Dependency**: Phase 1 + Phase 2 완료 후 시작

## Active

(없음)

## Pending

### 1. 바이너리 관리

- [ ] `infrastructure/outbound/process_manager/binary.rs`
  - `LLAMA_SERVER_VERSION` 상수 (예: "b5000")
  - `binary_path() -> PathBuf` — `{MODEL_LOCAL_PATH}/bin/llama-server`
  - `check_installed() -> bool`
  - `install(version) -> Result`
    - GitHub Releases API: `https://api.github.com/repos/ggml-org/llama.cpp/releases/tags/{version}`
    - 플랫폼 감지 (linux-x86_64, macos-arm64 등)
    - 바이너리 다운로드 + chmod +x

### 2. 포트 풀

- [ ] `infrastructure/outbound/process_manager/port_pool.rs`
  - `PortPool { range: 11430..=11530, in_use: DashSet<u16> }`
  - `allocate() -> Result<u16>`
  - `release(port: u16)`

### 3. 프로세스 생명주기

- [ ] `infrastructure/outbound/process_manager/process.rs`
  - `LlamaServerConfig { model_path, ctx_size, parallel, port, gpu_layers, cpu_threads }`
  - `LlamaServerProcess { child: Child, config, started_at }`
  - `start(config) -> Result<Self>`
    - binary.check_installed()? install()
    - Command::new("llama-server").args([...]).spawn()
    - wait_healthy(30s) — GET /health 폴링 1s 간격
  - `stop(self) -> Result` — SIGTERM → 5s → SIGKILL
  - `is_alive() -> bool` — child.try_wait() 확인
  - `health() -> SlotStatus`

### 4. 프로세스 매니저

- [ ] `infrastructure/outbound/process_manager/manager.rs`
  - `ProcessManager { registry: DashMap<Uuid, LlamaServerProcess>, port_pool, binary }`
  - `load_model(model_path, config) -> Result<LlmProvider>`
    1. port_pool.allocate()
    2. process.start(config)
    3. LlmProvider 자동 DB 등록 (ProviderType::LlamaServer)
    4. registry.insert
  - `unload_model(provider_id) -> Result`
    1. process.stop()
    2. port_pool.release()
    3. DB provider 제거 또는 status=offline
  - `list_running() -> Vec<ProcessInfo>`
  - `auto_recover()` — 10s 루프, 크래시 감지 후 재시작

### 5. 모델 로드 API

- [ ] `infrastructure/inbound/http/inference_load_handlers.rs`
  - `POST /v1/inference/load`
    ```json
    { "hf_repo": "Qwen/...", "quantization": "q4_k_m",
      "ctx_size": 32768, "parallel": 4, "gpu_layers": -1 }
    ```
    → resolver.resolve() + manager.load_model() → SSE 진행 스트리밍
  - `DELETE /v1/inference/{provider_id}` → manager.unload_model()
  - `GET /v1/inference/running` → manager.list_running()

- [ ] 라우터에 엔드포인트 추가

### 6. 배경 루프 등록

- [ ] `bootstrap/background.rs`
  - `process_manager.auto_recover()` 루프 등록 (10s)

### 7. Ollama 레거시 코드 정리

- [ ] `ollama/lifecycle.rs` — deprecated 마킹 + TODO 주석
- [ ] `ollama/preloader.rs` — deprecated 마킹
- [ ] `ollama/capability.rs` — deprecated 마킹
- [ ] `OllamaModelManager` — deprecated 마킹
- [ ] 실제 삭제는 Ollama provider 사용자 0 확인 후 (별도 cleanup PR)

### 8. CDD 문서

- [ ] `docs/llm/providers/process-manager.md` 신규 작성

## Completed

(없음)

## Blocked

Phase 1 + Phase 2 완료 대기
