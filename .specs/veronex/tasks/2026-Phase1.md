# Tasks: 2026-Phase1 — LlamaServer 어댑터

> L3: ADD 자율 실행 | scopes/2026-Phase1.md 기반 | **Created**: 2026-05-09

## Active

(없음 — 시작 전)

## Pending

### 1. 도메인 확장

- [ ] `domain/enums.rs` — `ProviderType` 에 `LlamaServer` 추가
  - `#[serde(rename = "llama_server")]`
  - `Display` impl 업데이트
  - 기존 `Ollama`, `Gemini` 유지

### 2. LlamaServerAdapter 신규 구현

- [ ] `infrastructure/outbound/llama_server/mod.rs` 생성
- [ ] `infrastructure/outbound/llama_server/health.rs` 생성
  - `SlotStatus { slots_idle: u32, slots_processing: u32 }`
  - `get_health(client, base_url) -> Result<SlotStatus>`
  - GET `{url}/health` → `{"status":"ok","slots_idle":N,"slots_processing":M}`
- [ ] `infrastructure/outbound/llama_server/adapter.rs` 생성
  - `LlamaServerAdapter { base_url, client }`
  - `impl ModelLifecyclePort`:
    - `ensure_ready`: GET /health → ok? AlreadyLoaded
    - `instance_state`: SlotStatus → ModelInstanceState
    - `evict`: no-op (프로세스 종료는 ProcessManager 담당)
  - `impl InferenceProviderPort`:
    - `infer`: POST /v1/chat/completions (non-streaming)
    - `stream_tokens`: POST /v1/chat/completions (SSE 스트리밍)
      - `data: {...}` 라인 파싱
      - `[DONE]` 감지
      - StreamToken 변환 (OllamaAdapter 대비 단순)
  - wiremock 단위 테스트 (health ok/fail, stream, infer)

### 3. 라우터 확장

- [ ] `provider_router.rs` 수정
  - `make_adapter()`: `ProviderType::LlamaServer → LlamaServerAdapter::new(url)` 분기 추가
  - `pick_best_provider()`: `LlamaServer` 분기 추가
    - `get_llama_server_slots(provider, valkey)` 함수 신규
    - slots_idle 최대 서버 선택
  - `get_llama_server_slots()`: GET /health → slots_idle, 에러 시 0

### 4. 헬스체커 확장

- [ ] `health_checker.rs` 수정
  - `LlamaServer` 분기: GET `{url}/health` (timeout 5s)
  - 기존 Ollama 분기 유지

### 5. capacity/analyzer.rs 확장

- [ ] `capacity/analyzer.rs` 수정
  - sync loop에 `LlamaServer` 분기 추가
  - GET `/health` → slots 정보 수집
  - 기존 Ollama sync loop 유지

### 6. DB Migration

- [ ] 마이그레이션 파일 생성 (`docker/postgres/migrations/`)
  ```sql
  ALTER TABLE llm_providers
    DROP CONSTRAINT IF EXISTS llm_providers_provider_type_check,
    ADD CONSTRAINT llm_providers_provider_type_check
      CHECK (provider_type IN ('ollama', 'gemini', 'llama_server'));
  ```

### 7. API 핸들러 확장

- [ ] `provider_handlers.rs` 수정
  - `RegisterProviderRequest`: `LlamaServer` type 허용
  - validation: LlamaServer는 url 필수, api_key 불필요
  - `ProviderSummary` 응답에 `LlamaServer` 표시

### 8. CDD 문서 업데이트

- [ ] `docs/llm/providers/` 에 `llama-server.md` 신규 작성
  - 등록 방법, slots 라우팅, health 엔드포인트
- [ ] `docs/llm/flows/model-lifecycle.md` LlamaServer 섹션 추가
- [ ] `.ai/architecture.md` 포트 테이블 업데이트

## Completed

(없음)

## Blocked

(없음)

## 완료 기준 체크리스트

- [ ] `cargo test` 전체 통과
- [ ] LlamaServer provider 등록 API 동작
- [ ] 실제 llama-server 대상 추론 요청 성공
- [ ] 기존 Ollama provider 동작 영향 없음
- [ ] DB migration 적용 확인
