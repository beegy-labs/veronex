# Tasks: 2026-Phase2 — ModelStore (HuggingFace + S3 캐시)

> L3: ADD 자율 실행 | scopes/2026-Phase2.md 기반 | **Created**: 2026-05-09
> **Dependency**: 2026-Phase1 완료 후 시작

## Active

(없음)

## Pending

### 1. 환경변수 + 설정

- [ ] `bootstrap/config.rs` 에 추가
  ```
  VERONEX_MODEL_BUCKET       (필수)
  VERONEX_MODEL_LOCAL_PATH   (기본: /var/veronex/models)
  VERONEX_MODEL_MAX_DISK_GB  (기본: 200)
  HF_TOKEN                   (선택)
  ```

### 2. HuggingFace 클라이언트

- [ ] `infrastructure/outbound/model_store/hf_client.rs`
  - `HfClient { client: reqwest::Client, token: Option<String> }`
  - `HfModelId { repo: String, filename: String, quantization: String }`
  - `list_gguf_files(repo) -> Vec<HfFileInfo>`
    - GET `https://huggingface.co/api/models/{repo}/tree/main`
    - type == "file" && path.ends_with(".gguf") 필터
  - `match_quantization(files, q) -> Option<HfFileInfo>`
    - q4_k_m → *Q4_K_M*.gguf (대소문자 무시)
  - `download_url(repo, filename) -> String`
  - `stream_download(url, dest_path) -> ProgressStream`
    - reqwest streaming → tokio::fs::File
    - yield DownloadProgress { bytes_done, total_bytes, pct }

### 3. S3 캐시

- [ ] `infrastructure/outbound/model_store/s3_cache.rs`
  - 기존 `s3/` 모듈 패턴 재사용 (image_store.rs 참고)
  - `s3_key(hf_repo, filename) -> String`
    - `models/{repo_escaped}/{filename}`
  - `exists(key) -> bool`
  - `download_to_local(key, dest) -> ProgressStream`
  - `upload_from_local(src, key) -> Result` (tokio::spawn 백그라운드)

### 4. 로컬 저장소

- [ ] `infrastructure/outbound/model_store/local_store.rs`
  - `LocalStore { base_path: PathBuf, max_disk_gb: u64 }`
  - `path_for(repo, filename) -> PathBuf`
  - `exists(repo, filename) -> bool`
  - `free_space_mb() -> u64`
  - `lru_evict_if_needed(required_mb: u64)` — 가장 오래된 파일 제거

### 5. 모델 리졸버 (오케스트레이터)

- [ ] `infrastructure/outbound/model_store/resolver.rs`
  - `ModelResolver { hf: HfClient, s3: S3Cache, local: LocalStore }`
  - `resolve(repo, quantization) -> Result<LocalModelPath>`
    1. local.exists? → 반환
    2. s3.exists? → s3.download_to_local → 반환
    3. hf.list_gguf → match_quantization → stream_download → local 저장
       → tokio::spawn(s3.upload) (논블로킹)
    4. 반환
  - `DownloadJob { id: Uuid, status, progress }` — 진행 상태 추적
  - `JobRegistry: DashMap<Uuid, DownloadJob>`

### 6. 다운로드 진행 API

- [ ] `infrastructure/inbound/http/model_store_handlers.rs`
  - `GET /v1/models/search?q=&limit=20` → HF 검색
  - `GET /v1/models/cached` → S3 목록
  - `POST /v1/models/download` → resolver.resolve() 시작 → job_id 반환
  - `GET /v1/models/download/{job_id}/status` → SSE 진행률
  - `DELETE /v1/models/cached/{repo}/{filename}` → S3 + 로컬 삭제

- [ ] `infrastructure/inbound/http/router.rs` 라우트 추가

### 7. 테스트

- [ ] `hf_client.rs` — wiremock HF API mock 테스트
- [ ] `s3_cache.rs` — localstack or mock S3 테스트
- [ ] `resolver.rs` — 전체 플로우 통합 테스트

### 8. CDD 문서

- [ ] `docs/llm/inference/model-store.md` 신규 작성
  - HF API, S3 캐시 정책, 로컬 디스크 LRU

## Completed

(없음)

## Blocked

Phase 1 완료 대기
