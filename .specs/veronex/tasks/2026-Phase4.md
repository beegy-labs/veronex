# Tasks: 2026-Phase4 — AIMD slots_idle 재구현

> L3: ADD 자율 실행 | scopes/2026-Phase4.md 기반 | **Created**: 2026-05-09
> **Dependency**: Phase 1 + Phase 2 + Phase 3 완료 후 시작

## Active

(없음)

## Pending

### 1. SlotPool (VramPool 대체)

- [ ] `infrastructure/outbound/capacity/slot_pool.rs` 신규
  - `SlotPool { pool: DashMap<Uuid, SlotState> }`
  - `SlotState { slots_idle, slots_processing, last_updated }`
  - `update(provider_id, status: SlotStatus)`
  - `available_slots(provider_id) -> u32`
  - `best_provider(providers) -> Option<Uuid>` — slots_idle 최대

- [ ] `ports/outbound/concurrency_port.rs` 수정
  - `VramPoolPort` → `SlotPoolPort` (또는 병렬 추가)
  - VRAM MB 기반 메서드 제거 또는 deprecated

### 2. AIMD 재구현

- [ ] `capacity/analyzer.rs` AIMD 섹션 재작성
  - Ollama LLM 배치 분석 루프 제거
  - llama-server 분기:
    ```
    load_ratio = slots_processing / total_slots
    if load_ratio > 0.8 → record "increase" 이벤트
    if error_rate > 0.1 → record "decrease" 이벤트
    if load_ratio > 0.95 and vram_free → "scale_out" 이벤트
    if load_ratio < 0.1 and instance_count > 1 → "scale_in" 이벤트
    ```
  - AIMD 이벤트 → ProcessManager 액션 연결

### 3. 라우팅 업데이트

- [ ] `provider_router.rs`
  - `pick_best_provider()` LlamaServer 분기 → SlotPool 조회
  - VRAM MB 기반 점수 → slots_idle 기반 점수

### 4. 스케일 아웃/인 연동

- [ ] `capacity/analyzer.rs` → `ProcessManager` 호출
  - `scale_out(model_config)` — 동일 모델 새 인스턴스 시작
  - `scale_in(provider_id)` — 유휴 인스턴스 graceful 종료
    - 진행 중 슬롯 0될 때까지 대기 후 종료

### 5. DB 정리

- [ ] migration: `model_vram_profiles` 테이블 deprecated (삭제 아님)
- [ ] migration: `llm_providers.num_parallel` 컬럼 — AIMD 관리로 전환
  - 사용자 설정 초기값으로만 사용, 이후 AIMD가 동적 조정

### 6. ClickHouse 메트릭

- [ ] migration: `inference_capacity_events` 테이블 추가
  ```sql
  CREATE TABLE inference_capacity_events (
    timestamp        DateTime,
    provider_id      UUID,
    model            String,
    slots_idle       UInt32,
    slots_processing UInt32,
    load_ratio       Float32,
    parallel_count   UInt32,
    action           String  -- 'increase','decrease','scale_out','scale_in','noop'
  ) ENGINE = MergeTree() ORDER BY (timestamp, provider_id);
  ```
- [ ] analyzer.rs → ClickHouse capacity_events 기록

### 7. 대시보드 업데이트

- [ ] Frontend: VRAM 차트 → Slots 차트 (slots_idle/processing 시계열)
- [ ] Provider 목록: VRAM MB → slots 현황 표시
- [ ] AIMD 이벤트 히스토리 위젯 추가

### 8. CDD 문서

- [ ] `docs/llm/inference/capacity.md` 전면 개정
  - VRAM 기반 섹션 → Slots 기반으로 교체
  - AIMD 새 알고리즘 문서화

## Completed

(없음)

## Blocked

Phase 3 완료 대기
