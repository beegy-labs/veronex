{{- if .Values.valkey.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "inferq.fullname" . }}-valkey
  labels:
    {{- include "inferq.labels" . | nindent 4 }}
    app.kubernetes.io/component: valkey
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.valkey.storage.storageClassName }}
  storageClassName: {{ .Values.valkey.storage.storageClassName | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.valkey.storage.size | quote }}
{{- end }}
