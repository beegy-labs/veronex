{{- if and .Values.weatherMcp.enabled .Values.weatherMcp.autoscaling.enabled (not .Values.weatherMcp.autoscaling.keda) }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-weather-mcp
  labels:
    {{- include "veronex.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-weather-mcp
  minReplicas: {{ .Values.weatherMcp.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.weatherMcp.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.weatherMcp.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
