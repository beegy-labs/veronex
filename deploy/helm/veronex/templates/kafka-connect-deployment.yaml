{{- if .Values.kafkaConnect.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-kafka-connect
  labels:
    {{- include "veronex.labels" . | nindent 4 }}
    app.kubernetes.io/component: kafka-connect
spec:
  replicas: {{ .Values.kafkaConnect.replicas | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Release.Name }}-kafka-connect
  template:
    metadata:
      labels:
        {{- include "veronex.labels" . | nindent 8 }}
        app.kubernetes.io/name: {{ .Release.Name }}-kafka-connect
    spec:
      {{- if .Values.serviceAccount.create }}
      serviceAccountName: {{ .Values.serviceAccount.name | default (printf "%s-veronex" .Release.Name) }}
      {{- end }}
      containers:
        - name: kafka-connect
          image: "{{ .Values.kafkaConnect.image.repository }}:{{ .Values.kafkaConnect.image.tag }}"
          imagePullPolicy: {{ .Values.kafkaConnect.image.pullPolicy | default "IfNotPresent" }}
          args:
            - run
            - /etc/kafka-connect/config.yaml
          ports:
            - name: http
              containerPort: 4195
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          env:
            - name: CLICKHOUSE_HTTP_URL
              value: {{ .Values.kafkaConnect.clickhouse.httpUrl | quote }}
            - name: CLICKHOUSE_DB
              value: {{ .Values.kafkaConnect.clickhouse.database | quote }}
            - name: CLICKHOUSE_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.kafkaConnect.clickhouse.secretName }}
                  key: username
            - name: CLICKHOUSE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.kafkaConnect.clickhouse.secretName }}
                  key: password
            {{- if ne (.Values.kafkaConnect.kafka.securityProtocol | default "PLAINTEXT") "PLAINTEXT" }}
            - name: KAFKA_SASL_MECHANISM
              value: {{ .Values.kafkaConnect.kafka.saslMechanism | quote }}
            - name: KAFKA_USERNAME
              value: {{ .Values.kafkaConnect.kafka.saslUsername | quote }}
            - name: KAFKA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.kafkaConnect.kafka.secretName }}
                  key: password
            {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/kafka-connect
              readOnly: true
          resources:
            {{- toYaml .Values.kafkaConnect.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ .Release.Name }}-kafka-connect-config
{{- end }}
