{{- if .Values.veronexAgent.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-veronex-agent
  labels:
    {{- include "veronex.labels" . | nindent 4 }}
    {{- include "veronex.agent.selectorLabels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "veronex.agent.selectorLabels" . | nindent 4 }}
{{- end }}
